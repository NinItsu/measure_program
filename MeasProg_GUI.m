function [imp] = MeasProg_GUI(setting)

% sig.TYPE;sig.A;sig.t;
% sig.L=sig.t*FS;
% [sig.s sig.inv]=meas_sig_gen(sig,FS);

% Static
% [imp] = MeasProg_static(FS,DEVICE_ID,DLY,NO_LSP,NO_MIC,TRAIL,WARM_UP,imp.L,sig,meas_name);

% Turntable
% rot.MIN;rot.MAX;rot.INR;
% [imp] = MeasProg_turntable(FS,DEVICE_ID,DLY,NO_LSP,NO_MIC,TRAIL,WARM_UP,rot,imp.L,sig,meas_name);

% Actuator
% act.MIN_X;act.MAX_X;act.INR_X;act.MIN_Z;act.MAX_Z;act.INR_Z;
% [imp] = MeasProg_turntable(FS,DEVICE_ID,DLY,NO_LSP,NO_MIC,TRAIL,WARM_UP,act,imp.L,sig,meas_name);
end